{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run SomaticVR Tracker Emulator",
            "type": "coreclr",
            "request": "launch",
            "program": "${workspaceFolder}/bin/Debug/net9.0/SomaticVR-Tracker-Emulator.dll",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "stopAtEntry": false,
            "justMyCode": false
        }
    ]
}